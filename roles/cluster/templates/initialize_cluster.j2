#!/bin/bash

master_host_string="{% for host in groups['all'] %} cluster meet {{ hostvars[host]['inventory_hostname'] }} {{ redis_port[0] }} \n {% endfor %}"

echo -e cluster meet $master_host_string | {{ redis_bin_dir }}/redis-cli -p {{ redis_port[0]}}